name: Linux Targets

on:
  push:
    branches:
    - 'master'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: "Build sitl"
        run: "make px4_sitl none_custom_quad"
